document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('.top nav').classList.toggle('open'));const input=document.getElementById('skillSearch');if(input){const cards=[...document.querySelectorAll('.searchable')],count=document.getElementById('count'),empty=document.getElementById('noResults');const run=()=>{const q=input.value.trim().toLowerCase();let n=0;cards.forEach(c=>{const ok=!q||c.innerText.toLowerCase().includes(q);c.style.display=ok?'block':'none';if(ok)n++});count.textContent=n+' קטגוריות';empty.style.display=n?'none':'block'};input.addEventListener('input',run);run();}